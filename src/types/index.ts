import type { LucideIcon } from 'lucide-react';

export type Service = {
  id: string;
  name: string;
  icon: LucideIcon;
};

export type ContactInfo = {
  email: string;
  phoneMonica: string;
  phoneFabio: string;
};
